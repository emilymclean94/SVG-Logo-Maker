class Shape {
    constructor(color) {
        this.color = color;
    }
    setColor(color) {
        this.color = color;
    }
}

class Circle extends Shape {
    render() {
        /*
            Return a string that represents a circle element, using literals to pass color.
        */
    }
}

class Triangle extends Shape {
    render() {
        /*
        Return a string that represents a circle element, using string literals to pass color.
        */
    }
}

class Square extends Shape {
    render() {
        /*
        Return a string that represents a circle element, using string literals to pass color.
        */
    }
}