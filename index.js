const inquirer = require('inquirer');

class CLI {
    run() {
        return inquirer
            .prompt ([
                /*
                    Prompt the user for the specs they want displayed in the svg
                */
            ])
    }
}